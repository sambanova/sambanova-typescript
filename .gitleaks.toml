# Gitleaks config for sambanova-typescript. Extends the built-in default ruleset.
title = "sambanova-typescript gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Stainless-generated SDK metadata (not credentials)"
paths = [
  '''\.stats\.yml$''',
]
