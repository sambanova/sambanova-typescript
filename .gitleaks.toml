# Gitleaks config for sambanova-typescript-staging.
# Extends the built-in default ruleset. Add [allowlist] entries here
# as false positives surface during the non-blocking rollout. Example:
#
# [allowlist]
# description = "Known-safe paths"
# paths = ['''docs/.*\.md$''']
title = "sambanova-typescript-staging gitleaks config"

[extend]
useDefault = true
